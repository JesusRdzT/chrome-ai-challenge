// import { createMainDialog } from './extension-components.js';

export function startMainExperience() {
	/*
	const { node: mainDialog, ...mainBuilder } = createMainDialog();
	console.log("shows the dialog");
	mainDialog.show();
	*/
}
